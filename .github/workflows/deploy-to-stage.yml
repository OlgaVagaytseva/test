name: Stage Deployment 
on:
  workflow_dispatch:
jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: Stage
    steps:
      - name: Deploy to Stage
        run: echo "Deploy to Stage"